{
  "id": "01a06478-f4f4-725a-8bc0-776641f7a436",
  "name": "transit-thing",
  "version": "0.1.0",
  "description": "Next arrivals for your stops, from a Transit Tracker server (tt.horner.tj by default). Reads this device's location only when you ask for nearby stops.",
  "icon": "icon.svg",
  "settings": "settings.html",
  "config": [
    {
      "type": "string",
      "data": {
        "key": "apiBaseUrl",
        "label": "Transit Tracker API base URL",
        "pattern": "^https://",
        "minLength": null,
        "maxLength": null,
        "default": "https://tt.horner.tj/"
      }
    },
    {
      "type": "string",
      "data": {
        "key": "feed",
        "label": "Feed code (st is Puget Sound)",
        "pattern": null,
        "minLength": null,
        "maxLength": null,
        "default": "st"
      }
    },
    {
      "type": "string",
      "data": {
        "key": "slots",
        "label": "Stops as JSON: [{stopId, stopName, routeIds}] (blank uses the stops picked on the device)",
        "pattern": null,
        "minLength": null,
        "maxLength": 4000,
        "default": ""
      }
    },
    {
      "type": "number",
      "data": {
        "key": "perStop",
        "label": "Arrivals shown per stop",
        "min": 1,
        "max": 4,
        "step": 1,
        "default": 3
      }
    },
    {
      "type": "boolean",
      "data": {
        "key": "ambientIdle",
        "label": "Show the clock after a minute without input",
        "default": true
      }
    }
  ],
  "permissions": [
    "geo"
  ],
  "art": {
    "heroPx": 248,
    "thumbPx": 96
  }
}
